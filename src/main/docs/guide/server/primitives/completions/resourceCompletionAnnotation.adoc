The preferred way to declare a resource completion is using a method annotated with api:mcp.annotations.ResourceCompletion[] in a `@Singleton` bean.

snippet::io.micronaut.mcp.docs.completions.resources.MyResourceCompletions[project-base="test-suite-docs", tags="clazz"]

You can use resource completions, for example, in combination with Resource templates (see the uri for both the resource template and resource completion match):

snippet::io.micronaut.mcp.docs.completions.resources.MyResourceTemplates[project-base="test-suite-docs", tags="clazz"]
