Alternatively, you define resources registering beans (typically in a https://docs.micronaut.io/latest/guide/#factories[bean factory]).

NOTE: The following example assumes you set `SYNC` as your server type. Because of that, the following example creates defining resources with the class `McpStatelessServerFeatures.SyncResourceSpecification`. <<primitivesClassesPerServerType, If you used a different server type, you should use a different class>>.

snippet::io.micronaut.mcp.docs.resources.ResourcesFactory[project-base="test-suite-docs", tags="fakepackage,imports,clazz"]
